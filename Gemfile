source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem.files = Dir["lib/**/*"] + Dir["app/**/*"]

# Specify your gem's dependencies in mumuki-styles.gemspec
gemspec
