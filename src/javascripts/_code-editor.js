mumuki.load(function () {

  // var CODE_EDITOR = 'code-editor';
  // var CODE_EDITOR_SELECTOR = 'textarea.' + CODE_EDITOR;

  // var $originalEditor = $(CODE_EDITOR_SELECTOR);

  // var $editor = $originalEditor.clone();

  // var $div = $([
  // ].join(''));

  // $originalEditor.replaceWith($div)

});
